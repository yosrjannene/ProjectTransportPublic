<form method="post" action="{{ path('moy_transport_delete', {'id': moy_transport.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ moy_transport.id) }}">
    <button class="btn">Delete</button>
</form>
