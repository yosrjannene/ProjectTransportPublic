{% extends 'base.html.twig' %}

{% block title %}MoyTransport{% endblock %}

{% block body %}
    <h1>MoyTransport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ moy_transport.id }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ moy_transport.price }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ moy_transport.name }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ moy_transport.picture }}</td>
            </tr>
            <tr>
                <th>VilleDepart</th>
                <td>{{ moy_transport.villeDepart }}</td>
            </tr>
            <tr>
                <th>VilleArrive</th>
                <td>{{ moy_transport.villeArrive }}</td>
            </tr>
            <tr>
                <th>Description5</th>
                <td> {{ moy_transport.Description |replace({'bro': '*', 'tit': "*"})}}</td>

            </tr>
        </tbody>
    </table>

    <a href="{{ path('moy_transport_index') }}">back to list</a>
    <a href="{{ path('reservation3_new', {'id': moy_transport.id}) }}">New Reservation</a>
    <a href="{{ path('moy_transport_edit', {'id': moy_transport.id}) }}">edit</a>

    {{ include('moy_transport/_delete_form.html.twig') }}
    <iframe
            width="600"
            height="450"
            style="border:0;width:100%;"
            margin="10px 70px"
            loading="lazy"
            allowfullscreen
            src="https://www.google.com/maps/embed/v1/place?key=AIzaSyDX-nRJ8JxhkpsCsD_-7qGO1vgGtRfMWG4
    &q={{moy_transport.villeDepart}},la gare">
    </iframe>
{% endblock %}
